services:
  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    image: pomodoro-api:latest
    container_name: pomodoro-api
    restart: unless-stopped
    ports:
      - '3000:3000'
    environment:
      NODE_ENV: production
      PORT: 3000
